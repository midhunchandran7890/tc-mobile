import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#3B7D6E",
    alignItems: "center",
    paddingVertical: 15,
    borderRadius: 10,
  },
  label: {
    color: "#FFFFFF",
    fontSize: 18,
    fontWeight: "bold",
  },
});
